# Security

Do not commit browser profiles, cookies, login databases, session files, API
keys, customer data, or confidential product materials.

The browser profile used by this project is stored outside the repository:

```text
%LOCALAPPDATA%\CreativePipelineV3\EdgeProfile
```

If such data was committed accidentally, remove it from Git history and rotate
or revoke affected credentials before publishing the repository.
