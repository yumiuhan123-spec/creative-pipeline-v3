# 版本管理器设计占位

`3.1.0` 暂时移除临时本地小版本发布脚本。

后续计划在这里实现完整版本管理器，目标是：

1. 读取 Git diff、文件变化和当前 `VERSION.json`。
2. 调用 DeepSeek API 分析更新内容。
3. 自动建议版本号：
   - patch
   - minor
   - major
4. 自动生成 changelog 草稿。
5. 生成本地版本档案快照。
6. 避免提交浏览器数据、真实产品素材和运行结果。

原则：

```text
版本管理器是独立模块，不应该塞进主工作流 P1-P5。
```
